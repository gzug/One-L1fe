// Deleted — demo info moved to AppHeader info icon overlay (Batch F3b).
// File retained as empty module to avoid breaking any stale import resolution cache.
// Safe to fully remove once confirmed no imports remain.
export {};
